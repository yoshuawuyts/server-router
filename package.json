{
  "name": "server-router",
  "version": "6.0.0",
  "description": "Server router",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/server-router",
  "keywords": [
    "server",
    "router",
    "trie",
    "wayfarer"
  ],
  "license": "MIT",
  "dependencies": {
    "pathname-match": "^1.2.0",
    "urlencode": "^1.1.0",
    "wayfarer": "^7.0.0"
  },
  "devDependencies": {
    "concat-stream": "^2.0.0",
    "dependency-check": "^4.1.0",
    "get-server-port": "^1.0.0",
    "istanbul": "^0.4.5",
    "standard": "^14.3.1",
    "tape": "^4.11.0"
  }
}
